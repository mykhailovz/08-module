# how to do testing with cypress

### install cypress
```
$ npm i cypress
```

### open cypress in of these ways
```
$ npx cypress open
$ ./node_modules/.bin/cypress open
$ $(npm bin)/cypress open
```
